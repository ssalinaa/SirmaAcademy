function fuelEfficiency(distance, fuel) {
    let totalFuel = distance / fuel;
    console.log(totalFuel.toFixed(2));
}

fuelEfficiency(500, 40);
fuelEfficiency(600, 40);
fuelEfficiency(750, 40);